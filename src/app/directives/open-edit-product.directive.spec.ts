import { OpenEditProductDirective } from './open-edit-product.directive';

describe('OpenEditProductDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenEditProductDirective();
    expect(directive).toBeTruthy();
  });
});
